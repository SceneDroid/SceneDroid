package android.goal.explorer.analysis.components;

import android.goal.explorer.data.android.constants.MethodConstants;

public class DrawerComponentAnalyzer{
    // public SootMethod sootMethod;

    // public DrawerComponentAnalyzer(SootMethod method){

    // }

    // public void getRegisteredDrawers(){
    //     if(currentActivity.xml().contains(DrawerLayout.resourceInstance())){
    //         //store into the activity resources ?
    //         //add to screen when visible only
    //         if(currentActivity.onCreate().contains(addDrawerListener())){
    //             //is this necessary ?
    //         }
    //         //else if(currentActivity.)
    //     }
    // }

    // public void getVisibleDrawers(){
    //     if(currentActivity.containsDrawer()){
    //         if(sootMethod.contains(MethodConstants.DrawerOpeningMethods())){
    //             //return whichever representation of drawers was picked
    //         }
    //         else{ //what is the other way to open a drawer ?

    //         }
    //     }
    //     // if(onCreate.contains(MethodConstants.DrawerRegistrationMethods)){

    //     // }
    //     // else if(currentActivity.xml().contains(NavigationGraph.instance())){

    //     // }
    // }

    // /**
    //  * Depends on the type of drawer (with a navigationview and menu, with a nav graph, )
    //  * First parse nav graph, then check if there is available nav graph
    //  */
    // public void parseDrawerOptionsItems(){
    //     //If no visiblerDrawer in the current screen, maybe add a screen with the drawer and make a fake transition onDrawerOpen with the resource id
    //     if(currentActivity.hasNavigationDrawer()){
    //         if(currentDrawer.hasMenu()){
    //             //mapping with the ids from the xml
    //             //or explicitely look for navigate function I guess
    //         }
    //     }
    //     else{
    //         //parse onNavigationSelectedItem ?
    //         //return map from item to 
    //     }
    // }
}